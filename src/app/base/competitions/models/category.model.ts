export interface ICompetitionCategory {
  id: number;
  displayName: string;

  lowerAge: number;
  upperAge: number;

  lowerWeight: number;
  upperWeight: number;

  lowerExperience: number;
  upperExperience: number;
}
